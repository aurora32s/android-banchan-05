<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="48dp"
    android:background="@color/greyScaleSurface">

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/bt_order"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/selector_button"
        android:textColor="@color/greyScaleWhite"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="28,220원 주문하기" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_notification"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:textColor="@color/greyScaleBlack"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/bt_order"
        app:layout_constraintStart_toStartOf="@id/bt_order"
        app:layout_constraintTop_toBottomOf="@id/bt_order"
        tools:text="남은 금액" />


</androidx.constraintlayout.widget.ConstraintLayout>